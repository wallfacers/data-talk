Expression ((Projection + Before ORDER BY))
  ReadFromMergeTree (default.orders)
  Indexes:
    PrimaryKey
      Keys: user_id
      Condition: and(equals(user_id, 42), 1)
      Parts: 1/3
      Granules: 5/100